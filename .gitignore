# OS
.DS_Store

# Claude Code workspace
.claude/
CLAUDE.md

# Vercel (site now deploys via GitHub Pages)
.vercel/

# Internal project materials — kept locally, not part of the deployed site
docs/
source-materials/
content-engine/
tooling/*
screenshots/
# ...except the site build source-of-truth (shared header/footer partials + build script),
# which must be version-controlled. Crawlers + crawl outputs stay ignored.
!tooling/build.py
!tooling/partials/
!tooling/partials/**

# Playwright MCP session artifacts
.playwright-mcp/

# Developer handover package (local only, never deploy)
handover/
